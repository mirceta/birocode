<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <UseWindowsForms>true</UseWindowsForms>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>ClaudeWeb</RootNamespace>
    <AssemblyName>ClaudeWeb</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="System.Management" Version="8.0.0" />
  </ItemGroup>

  <ItemGroup>
    <!-- Reused, shared Claude gateway client (openspec change discover-local-apps).
         Same client web-flow-autodev uses; needs the ClaudeMonitor gateway on :5123.
         The client project lives WITH the ClaudeMonitor backend in birokrat-ai-platform
         (it was previously mis-filed under a `prg/agentic-workflows` checkout). See
         openspec/changes/discover-local-apps/design.md (D2). -->
    <ProjectReference Include="..\..\birokrat-ai-platform\ClaudeMonitor\ClaudeMonitor.Client\ClaudeMonitor.Client.csproj" />
  </ItemGroup>

  <ItemGroup>
    <None Update="appsettings.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

  <ItemGroup>
    <!-- Canonical deploy scripts, seeded to the off-repo DeployScriptsDir on first
         run by DeployScriptProvisioner (makes the deploy procedure reproducible). -->
    <EmbeddedResource Include="Deploy\templates\*.tmpl" />
  </ItemGroup>

</Project>
